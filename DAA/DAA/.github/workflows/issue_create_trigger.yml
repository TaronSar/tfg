name: Create Project Trigger

on:
  issues:
    types:
    - opened
         
jobs:
  CREATE_ISSUES:
    uses: embention/sw_CI_libs/.github/workflows/issue_creator_project.yml@develop
    secrets: inherit
