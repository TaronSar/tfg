"""Privacy-preserving few-shot UAV identification package (KAI Vision).

The package is organised into a shared ``common`` layer (I/O, config loading,
constants, transforms) and domain subpackages (``model``, ``dataset``,
``train``, ``eval``, ``inference``, ``export``, ``preprocessing``).
"""
